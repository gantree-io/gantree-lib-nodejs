const extract = () => {
  return {
    substrate_user: 'subuser', // TODO(ryan): rename to system_user
    substrate_group: 'subgroup' // TODO(ryan): ryanme to system_group
  }
}

module.exports = {
  extract
}
